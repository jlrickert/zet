# How to view and edit pdfs and the command line

On a Debian based Linux distribution the program `pdftotext` can be installed from the package `poppler-utils`. From there you can open up a pdf with `less`.
