# Recommended regex syntax to use

See PCRE or perl compatible regular expressions.

Make sure it runs on busy box as this is the most common minimally used
container.

## See also

- http://www.pcre.org/
