# Vim filter cheatsheet

- `%!`: whole buffer
- `.!`: line
- `:!`: selection

# Cheatsheet

- `.!`

  Pipes the current line into the command. Replace line with stdout

- `:'<,'>.!`

  Pipe the selected lines into the command. Replace selected with stdout
